@{
    ViewData["Title"] = "Bem-vindo";
    var nome = ViewBag.NomeUsuario ?? User.Identity.Name;
}

<h2>Olá, @nome!</h2>
<p>Seja bem-vindo ao sistema 🚀</p>

<form asp-action="Logout" method="post">
    <button type="submit" class="btn btn-danger">Sair</button>
</form>
